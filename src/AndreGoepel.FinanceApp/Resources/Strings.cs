namespace AndreGoepel.FinanceApp.Resources;

/// <summary>
/// Marker for the app's own UI strings. Lives in the Resources namespace so its full name matches
/// the embedded resource path — no ResourcesPath needed.
/// </summary>
public sealed class Strings;
