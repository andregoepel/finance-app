version: 2
updates:
  # NuGet dependencies (versions are centrally managed in Directory.Packages.props).
  - package-ecosystem: nuget
    directory: "/"
    schedule:
      interval: weekly
    open-pull-requests-limit: 10
    groups:
      nuget-minor-patch:
        update-types:
          - minor
          - patch

  # GitHub Actions — keep the SHA-pinned actions current (Dependabot updates both
  # the pinned SHA and the version comment).
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly

  # Keep the tag@sha256 .NET base-image references current through reviewed PRs.
  - package-ecosystem: docker
    directory: "/src/AndreGoepel.FinanceApp"
    schedule:
      interval: weekly
