## Summary

- group Wise currency accounts by their provider connection/profile label
- keep separate household Wise connections in separate sections
- keep Wise accounts with missing or absent connections visible in an explicit unlinked section
- leave DKB, Revolut, CSV, cash, and crypto accounts in the ordinary flat account list
- extract the shared account grid so every section retains the same columns and lifecycle actions
- add English and German labels plus tested presentation ordering and fallback behavior
- add browser coverage using two real Wise connections, multiple currencies, and an unlinked Wise account

No account or provider documents are migrated or rewritten; the presentation uses the existing `ConnectionId` relationship.

## Validation

- `dotnet build AndreGoepel.FinanceApp.slnx -c Release --no-restore`
- `dotnet test tests/AndreGoepel.FinanceApp.Tests/AndreGoepel.FinanceApp.Tests.csproj -c Release --no-build --no-restore` (650 passed)
- `dotnet test tests/AndreGoepel.FinanceApp.E2ETests/AndreGoepel.FinanceApp.E2ETests.csproj -c Release --no-build --no-restore` (39 passed)

Closes #222
