<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Aspire.Hosting.Testing pulls RID-specific orchestration packages, so a committed
         lock file would be OS-dependent and break CI's locked-mode restore. The AppHost
         opts out of lock files for the same reason. -->
    <RestorePackagesWithLockFile>false</RestorePackagesWithLockFile>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Aspire.Hosting.Testing" />
    <PackageReference Include="Microsoft.Playwright" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" />
    <PackageReference Include="xunit.v3" />
    <PackageReference Include="xunit.runner.visualstudio">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="coverlet.collector">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
  </ItemGroup>

  <ItemGroup>
    <!-- The AppHost boots the real app graph (Postgres + MailHog + the Blazor web app). -->
    <ProjectReference Include="..\..\src\AndreGoepel.FinanceApp.AppHost\AndreGoepel.FinanceApp.AppHost.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The provider CSV/XLSX fixtures are shared with the Connectors tests; the import
         flow uploads one of them through the real InputFile control. -->
    <Content
      Include="..\fixtures\**\*.csv;..\fixtures\**\*.xlsx"
      LinkBase="fixtures"
      CopyToOutputDirectory="PreserveNewest"
    />
  </ItemGroup>
</Project>
